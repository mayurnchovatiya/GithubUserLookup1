Setup the developement environment and serve the application step.

1. Install Node.js and npm if they are not already on your machine.
2. Install the Angular CLI globally using 
	npm install -g @angular/cli
3. Go to the specific directory (github-user-lookup)
4. npm install   (to install dependencies)
5. ng serve --open
6. Enjoy!!